{
  "name": "fdf",
  "private": true,
  "scripts": {
    "build": "npm --prefix my-app install && npm --prefix my-app run build && npm --prefix server install && npm --prefix server run build",
    "start": "node server/dist/index.js",
    "dev": "npm --prefix server run dev",
    "dev:frontend": "npm --prefix my-app run dev"
  }
}
